RunLog — GitHub Action 실행 로그 패널. 인셋 카드 + 모노 12.5px/1.9. 단계 이름은 8자 폭으로 정렬(collect · generate · commit · deploy).
```jsx
<RunLog lines={[{cmd:true,text:'gh workflow run devlog.yml'},{text:'collect  · 3 repos, 1 active'},{cur:true,text:'vercel deploy'}]} />
```
