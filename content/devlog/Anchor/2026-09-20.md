---
title: "지우는 버튼을 큰 검은 버튼에서 내렸습니다"
titleEn: "Moved the delete button out of the big black slot"
date: "2026-09-20"
repo: "Anchor"
commits: 14
prs: 0
shas: [["aae0c76","docs: 라운드 2 판정에 PM 답을 반영 — 막힘 0, O02a 알약은 결함 아님, O03 라벨은 F03"],["23a6384","docs: F02~F11 점검 — 열린 리다이렉트와 지워지지 않는 음성 원본"],["96a238c","fix: F02 예시 자료가 붙여넣기 카드 안에 묻혀 있던 것"],["342c759","fix: 계정 삭제 확인에서 되돌릴 수 없는 쪽이 검은 주 버튼이었다"],["f964949","fix: 화면마다 h1 이 하나도 없던 것, 일본어 한자에 よみがな 가 없던 것"],["270c2dc","docs: QA 라운드 2(1차, 미리보기) 판정 — 막힘 2 · 헷갈림 2 · 다듬기 4"],["cb46f67","docs: RLS 가 실제로 남의 행을 막는 것을 확인함으로 올림"],["fa36af3","docs: QA 검증 항목 10건을 더하고 \"기준 문서에 없음\" 근거를 닫는다"],["2a46a97","design: 구현과 참고 화면을 맞대어 보고, 구현이 나은 두 가지를 참고에 반영"],["795c771","chore: QA 세션을 더 싼 모델로 교체하고 검증 항목 10건을 인계함"],["5ec6840","docs: 되돌릴 수 없는 일을 검은 버튼에서 내리고, 접근성 기준 두 줄을 세움"],["cbccff6","docs: QA 검증 항목에 기준 문서에 새로 들어온 규칙 셋을 반영한다"],["94441cd","design: 빠져 있던 화면 다섯 장 — 설정·계정 지우기 확인·홈 빈 상태·없는 화면·오류"],["0766814","chore: main 합침"]]
shasEn: [["aae0c76","docs: reflect PM answers in round 2 judgment — 0 blockers, O02a pill is not defective, O03 label is F03"],["23a6384","docs: review F02~F11 — open redirect and undeleted voice original"],["96a238c","fix: F02 example material was buried inside paste card"],["342c759","fix: account deletion confirmation had irreversible action on black primary button"],["f964949","fix: no h1 on each screen, Japanese kanji missing よみがな"],["270c2dc","docs: QA round 2 (1st, preview) judgment — 2 blockers · 2 confusions · 4 refinements"],["cb46f67","docs: confirm RLS actually blocks others' rows and promote"],["fa36af3","docs: add 10 QA validation items and close \"missing from reference document\" rationale"],["2a46a97","design: compare implementation with reference screens, reflect 2 superior implementations in reference"],["795c771","chore: replace QA session with cheaper model and hand off 10 validation items"],["5ec6840","docs: demote irreversible actions from black button and establish 2 accessibility criteria lines"],["cbccff6","docs: reflect newly added rule set from reference document in QA validation items"],["94441cd","design: 5 missing screens — settings · account deletion confirmation · home empty state · non-existent screen · error"],["0766814","chore: merge main"]]
---

## 뭘 했나

계정을 지우는 화면에서 버튼 두 개를 맞바꿨습니다. 크고 검은 버튼이 "계정 삭제"였는데, 이제 그 자리는 "아니, 그만둘래"입니다. 지우는 쪽은 아래 회색 글씨 링크로 내렸고, "삭제"라고 직접 입력해야 눌리게 했습니다.

빠져 있던 화면 5개도 새로 만들었습니다. 설정, 계정 삭제 확인, 카드가 하나도 없을 때의 첫 화면, 주소가 틀렸을 때, 오류가 났을 때입니다. 오류 화면은 그동안 영문 안내가 그대로 나오고 있었습니다.

화면 17개를 하나씩 열어 제목 줄이 한 곳에도 없던 것을 고쳤습니다. 화면을 소리로 읽어 주는 프로그램이 "지금 이 화면이 무엇에 대한 것인지"를 말할 수 있게 하는 줄입니다. 일본어 한자 위에 읽는 법이 들어갈 자리도 만들었습니다. 답을 보기 전에는 점선 빈칸인데, 칸 너비를 고정했습니다. 칸이 길어지면 정답이 몇 글자인지 새어 나가기 때문입니다.

## 왜

화면에 검은 버튼은 하나뿐이라, 사람들은 거기를 눈 감고 누릅니다. 습관적으로 눌렀는데 되돌릴 수 없는 일이 벌어지면 안 됩니다. 이 규칙은 계정 삭제에만 해당하는 게 아니라서, 되돌릴 수 없는 일 전부에 적용되도록 기준 문서에 적어 뒀습니다.

## 삽질 포인트

녹음한 음성이 실제로는 안 지워지고 있었습니다. 30일이 지나면 원본을 지운다고 해 뒀는데, 그 시각이 지나도 지우는 일을 하는 쪽이 아예 없었습니다. 계정 삭제는 더 나빴습니다. 계정을 지우면 녹음 목록이 먼저 사라지는데, 그 목록에 어떤 음성 파일인지가 적혀 있습니다. 목록이 없어지면 어느 파일을 지워야 하는지 영영 알 수 없습니다. 다 지웠다고 답해 놓고 주인 없는 녹음이 남는 셈입니다.

주소 검사에서도 구멍을 찾았습니다. 로그인 뒤 돌아갈 주소를 검사하는데, 빗금 방향 하나를 놓쳐서 외부 사이트 주소가 통과했습니다. 같은 검사 코드가 네 곳에 복사돼 있던 것이 원인이라, 한 곳으로 합쳐서 고칠 생각입니다.

## 다음 할 것

음성 원본을 실제로 지우는 일과 주소 검사 합치기를 처리합니다. 오늘 점검은 로그인 없이 겉모습만 본 것이라, 실제 계정으로 눌러 봐야 판정되는 항목은 다음 차례로 미뤘습니다. 설정 화면을 몇 묶음으로 나눌지는 그림과 기준 문서가 서로 달라서 결정을 기다립니다.

<!-- en -->

## What I did

On the account deletion screen, I swapped two buttons. The big black button used to say "Delete account". Now that slot says "No, never mind". Deleting moved down to a grey text link, and you have to type the word "delete" before it works.

I also built 5 screens that were missing: settings, the delete confirmation, the home screen when you have no cards yet, the wrong-address page, and the error page. Until today the error page showed an English message straight from the underlying tooling.

I opened all 17 screens and added the heading line that none of them had. It is the line that lets a screen reader say what the screen is about. I also made room above Japanese characters for their reading. Before you answer it shows a dotted blank, and I fixed the blank's width — a blank that stretches would leak how many letters the answer has.

## Why

There is only one black button per screen, so people press it without looking. Pressing it out of habit should never do something you cannot undo. That is not just about account deletion, so I wrote it into the standards document as a rule for anything irreversible.

## Rabbit holes

Voice recordings were never actually being deleted. We promise to remove the originals after 30 days, but nothing was doing the removing. Account deletion was worse. Deleting an account wipes the recording list first, and that list is what says which audio file belongs to whom. Once it is gone, there is no way to know which files to delete. We answer "deleted" and orphaned recordings stay behind.

I also found a hole in the address check. When you log in, we check the address you return to, and one slash direction slipped through, letting an outside address pass. The same check was copy-pasted in four places, so I will merge it into one.

## Next up

Actually delete expired audio, and merge the address check. Today's review only covered appearance without logging in, so anything that needs a real account to judge waits for the next pass. How settings should be grouped is still open — the mockup and the standards document disagree.
