---
title: "같은 일을 두 사람이 반대로 알고 있었습니다"
titleEn: "Two of us had the same step backwards"
date: "2026-09-21"
repo: "Anchor"
commits: 19
prs: 0
shas: [["477d528","docs: 그 상자는 파일이 아니라 옮겨 적은 것이다 — 파일이 오면 맞대어 볼 자리를 적는다"],["5c8cc7f","docs: 내가 「못 가른다」고 쓴 파일에 답이 스무 줄 아래 들어와 있었다"],["aff1f47","chore: main 을 가져와 합친다 — 밀기 전 ① (data/load-path-unknown)"],["42bc85a","docs: 못 여는 파일이 정하는 것을 내가 정한 것처럼 적었다 — 「프로덕션은 이쪽」을 뺀다"],["31f96ba","docs: 레포에 없는 파일 때문에 두 사람이 적재 길을 다르게 알고 있었다 — 그 파일이 뭘 돌리는지 박는다"],["b4f1da1","fix: 「낡은 엣지는 여기서 못 산다」가 틀렸다 — PM 축이 심어서 샀고 읽는 법이 하나 는다"],["20706cd","docs: 6,059 를 보고 「적재가 깨졌다」로 읽지 않게 — 위로 어긋난 수는 대개 실패가 아니다"],["e1d0e0e","docs: 적재 길이 둘인데 읽는 표는 하나였다 — SQL 로 가면 그 세 줄이 안 나온다"],["ffd6ac7","docs: 씨앗 적재가 어느 축에서 검증됐는지 적재하는 사람이 보는 자리에 적는다"],["6fe6515","docs: 수를 넘길 때 「어느 축에서 셌나」를 같이 적는다"],["003923e","docs: 수가 같다고 같은 것이 아니다 — 같은 날 두 자리가 같은 자리에 닿았다"],["5c3a109","chore: main 을 가져와 합친다 — 밀기 전 ① (claude/serene-pasteur-hflx6o)"],["a4739cb","feat: 밀기 ② 가 빨갈 때 살리는 명령을 같이 찍는다"],["77fcdc7","chore: main 을 가져와 합친다 — 밀기 전 ① (data/seed-comment-stamp)"],["f2dcbfe","docs: 수 옆에 「움직이는 표식」을 박아 놨다 — origin/main 은 내일이면 다른 나무다"],["6c55300","docs: 다섯째 모양 — 적을 때는 맞았는데 20분 뒤에 낡았다. 이건 「열어 봐라」로 안 잡힌다"],["c72966e","docs: 세어 보고도 틀리는 네 가지 모양을 10장에 적는다"],["b037cfe","chore: main 을 가져와 합친다 — 밀기 전 ① (claude/affectionate-brahmagupta-947f48)"],["8f66b17","docs: 프로덕션에서 읽은 수에는 읽은 시각을 붙인다 + 막힌 자리를 적는다"]]
shasEn: [["477d528","docs: The box is a transcription, not a file — write where to compare when the file arrives"],["5c8cc7f","docs: The answer was twenty lines below in the file I wrote 'cannot distinguish'"],["aff1f47","chore: Fetch and merge main — before push ① (data/load-path-unknown)"],["42bc85a","docs: I wrote as if I decided what the unopenable file determines — remove 'production is here'"],["31f96ba","docs: Two people understood the load path differently because of a file missing from the repo — document what that file runs"],["b4f1da1","fix: 'Old edge cannot be purchased here' is wrong — PM axis was planted and purchased, one way to read it"],["20706cd","docs: Do not read 'load broken' when seeing 6,059 — upward offset numbers are usually not failures"],["e1d0e0e","docs: Two load paths but one read table — when going to SQL those three lines don't appear"],["ffd6ac7","docs: Write where the load operator sees seed load validation axis — in the operator's viewing area"],["6fe6515","docs: Write 'which axis was counted' together when passing numbers"],["003923e","docs: Same numbers don't mean same thing — on the same day two places touched the same spot"],["5c3a109","chore: Fetch and merge main — before push ① (claude/serene-pasteur-hflx6o)"],["a4739cb","feat: Print rescue command together when push ② is fast"],["77fcdc7","chore: Fetch and merge main — before push ① (data/seed-comment-stamp)"],["f2dcbfe","docs: Embed 'moving marker' next to numbers — origin/main will be a different tree tomorrow"],["6c55300","docs: Fifth pattern — correct when writing but aged after 20 minutes. This isn't caught by 'open and check'"],["c72966e","docs: Write four patterns that are wrong even after counting on 10 sheets"],["b037cfe","chore: Fetch and merge main — before push ① (claude/affectionate-brahmagupta-947f48)"],["8f66b17","docs: Attach read time to numbers read from production + document blocked spots"]]
---

## 뭘 했나

학습 자료를 채워 넣는 절차의 설명서를 고쳤습니다. 하루 커밋 19개가 거의 다 문서였습니다. 자료를 넣는 방법, 잘 들어갔는지 확인하는 방법을 다시 적었습니다. 코드를 올리다 실패할 때 되살리는 명령을 화면에 같이 띄우게도 했습니다.

## 왜

자료를 넣는 길이 두 가지인데 사람마다 다른 쪽을 알고 있었습니다. 문제는 성공 판정이 정반대라는 점입니다. 한쪽 기준으로는 화면에 아무 메시지도 안 뜨는 게 정상이고, 다른 쪽 기준으로는 안 뜨면 실패입니다. 틀린 기준을 들고 기록을 읽으면 망가진 날을 멀쩡한 날로 읽게 됩니다.

## 삽질 포인트

어느 쪽이 맞는지 확인하려고 그 절차가 적힌 파일을 열려고 했습니다. 그런데 그 파일이 저장소에 아예 없었습니다. 올리는 과정에서 막혀서, 만든 사람이 담당자에게 직접 보낸 상태였습니다. "직접 열어서 확인한다"는 평소 원칙이 여기선 통하지 않았습니다. 그래서 화면 메시지 모양으로 성패를 가리는 규칙 자체를 뺐습니다. 어느 길로 갔든 저장한 곳에 직접 몇 개 들어 있는지 물어보면 되기 때문입니다. 저장된 개수가 예상보다 많이 나오는 경우도 있는데, 오래된 연결 정보가 아직 안 지워진 것이라 실패가 아니라고 적어 뒀습니다.

## 다음 할 것

지금 문서에 있는 절차 설명은 파일을 본 사람이 옮겨 적은 것입니다. 옮기는 사이에 한 줄이 빠졌을 수 있습니다. 그 파일이 저장소에 올라오는 날 원본과 맞대어 보기로 했고, 그때 잊지 않도록 해당 문서 바로 옆에 적어 뒀습니다.

<!-- en -->

## What I did

I rewrote the guide for loading study data. Nearly all 19 commits today were documentation. I rewrote how the data gets loaded and how to check that it landed. I also made the upload step print the recovery command when it fails.

## Why

There are two ways to load the data, and two of us each knew a different one. The trouble is that the success check is reversed between them. Under one, seeing no message on screen is normal; under the other, no message means it never ran. Read the logs with the wrong rule and a broken day looks fine.

## Rabbit holes

To settle it, I went to open the file that defines the procedure. It wasn't in the repository at all. It got blocked on the way up, so the author sent it to the owner directly. Our usual rule, "go open it and look," didn't apply here. So I dropped the whole idea of judging by what shows up on screen. Either way, you can just ask the storage how many items are in there. Sometimes the count comes out higher than expected; that's old link data not yet cleaned up, so I wrote down that it isn't a failure.

## Next up

The procedure now in the docs was copied out by someone who had seen the file. A line could have been lost in the copying. The day that file lands in the repository, I'll compare the two, and I wrote that reminder right next to the document so it won't be forgotten.
