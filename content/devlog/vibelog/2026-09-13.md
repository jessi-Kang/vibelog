---
title: "자동으로 올라가는 글에 생긴 구멍을 막았습니다"
titleEn: "Closed a hole in the auto-published posts"
date: "2026-09-13"
repo: "vibelog"
commits: 13
prs: 0
shas: [["d130d08","모티프가 next 밖에서도 그려지게 — 안 그러면 빈 화면 검사가 빈 화면을 통과시킨다"],["7567431","모티프 16종을 쇼츠 파이프라인에 넣는다"],["ac2588b","Lottie: 포인트 색만 테마 색으로 바꾼다"],["f639c4c","Lottie 후보를 고를 수 있게 먼저 거르는 도구"],["0f0ff96","그림 한 장짜리 화면을 폰 프레임에 넣지 않는다"],["3b49941","레드팀 스킬을 만들고 돌렸다 — 글 페이지 스크립트 이탈(XSS) 하나를 막았다"],["a463065","projects 배치가 30분마다 실패하던 것 — 쓴 파일을 다 담지 않았다"],["1dbe81b","영상 렌더를 죽인 node:fs를 걷어내고, 같은 일을 막는 검사를 붙인다"],["12fb4aa","쇼츠: 대본이 고른 화면을 녹화가 버리지 않는다"],["38c7a49","층 순서를 정한다 — 본문이 고정 탭바 위로 올라오던 것"],["965d39a","쇼츠 대본: 줄이려고 두 명사를 붙여 말을 만들지 않는다"],["71d0a65","쇼츠: 테두리는 가리키는 문장만, 가리킬 말은 화면에 하나뿐일 때만"],["fa161ed","문서: 취소선으로 먹는 물결표 표기를 걷어내고, 문서도 검사에 넣는다"]]
shasEn: [["d130d08","Draw motifs outside next as well — otherwise empty screen checks pass empty screens"],["7567431","Add 16 motifs to shorts pipeline"],["ac2588b","Lottie: Change only point color to theme color"],["f639c4c","Add filtering tool to select Lottie candidates first"],["0f0ff96","Don't fit single-image screens into phone frame"],["3b49941","Create and run red team skill — fixed one XSS vulnerability in text page script"],["a463065","Fix projects batch failing every 30 minutes — wasn't including all written files"],["1dbe81b","Remove node:fs that kills video render, add check to prevent same issue"],["12fb4aa","Shorts: Recording doesn't discard screens chosen by script"],["38c7a49","Define layer order — body was rising above fixed tab bar"],["965d39a","Shorts script: Don't create words by combining two nouns to shorten"],["71d0a65","Shorts: Border only for pointing sentences, pointing target only when there's one on screen"],["fa161ed","Docs: Remove strikethrough wavy mark notation, add docs to checks"]]
---

## 뭘 했나

사이트의 보안 구멍 하나를 찾아서 막았습니다. 이 블로그는 사람이 읽기 전에 글이 올라가는데, 제목에 특정 기호가 섞이면 브라우저가 그 뒤를 글자가 아니라 명령으로 읽어 버릴 수 있었습니다. 실제로 그런 제목을 붙인 임시 글을 만들어 재현했고, 확인한 뒤 지웠습니다. 이제는 어떤 기호가 들어와도 그냥 글자로만 보입니다. 페이지를 내보낼 때 같이 붙는 안전 설정 5개도 추가했습니다.

영상 쪽도 손봤습니다. 보여줄 화면도 설명 그림도 고르지 못한 문장이 배경과 자막만으로 지나가던 자리에, 글자 없는 작은 그림 16종을 넣었습니다. 가로로 긴 공유용 그림을 세로 휴대폰 틀에 억지로 끼워 넣어 양옆이 잘리던 것도 고쳤습니다.

## 왜

글도 영상도 사람 검토 없이 그대로 공개됩니다. 그래서 AI가 쓴 문장이 화면에 닿는 모든 자리가 위험한 자리입니다. 입력을 걸러 내는 대신 문장이 들어가는 자리 자체를 막았습니다. 걸러 내는 쪽은 다음 우회에서 집니다.

## 삽질 포인트

영상 만들기가 3번 연속 실패했습니다. 전날 넣은 검사 기능이 원인이었습니다. 그 기능을 영상 쪽이 함께 읽는 파일에 넣었는데, 영상을 그리는 프로그램은 그 안의 부품을 읽지 못합니다. 코드 점검은 멀쩡히 통과했고, 실제로 영상을 만들어 봐야만 드러나는 종류였습니다. 검사 기능을 다른 자리로 옮기고, 같은 실수를 미리 잡아 주는 점검을 하나 만들었습니다.

프로젝트 목록을 30분마다 갱신하는 자동 실행도 계속 실패하고 있었습니다. 실행할 때마다 파일 2개가 바뀌는데 저장 단계에서 1개만 챙겼고, 남은 파일이 걸려 저장이 거절됐습니다. 저장이 안 되니 다음 회차가 또 같은 자리에서 죽었습니다. 실패가 스스로를 반복시킨 셈입니다.

## 다음 할 것

- 장면에 넣을 움직이는 그림 후보 고르기 (쓸 수 있는 것만 걸러 내는 도구는 만들어 뒀습니다)
- 대본이 고른 화면을 녹화가 제대로 쓰는지 계속 확인하기

<!-- en -->

## What I did

I found and closed one security hole on the site. Posts here go live before any human reads them, and if a certain symbol slipped into a title, the browser could read what followed as an instruction instead of plain text. I reproduced it with a throwaway post that had such a title, confirmed it, then deleted it. Now any symbol shows up as text and nothing else. I also added 5 safety settings that ship with every page.

I worked on the videos too. Sentences that had no screen to show and no explanatory drawing used to pass by with just a background and subtitles. I added 16 small wordless drawings to fill that gap. I also fixed a wide sharing image that was being crammed into a tall phone frame and cropped on both sides.

## Why

Both the posts and the videos go public without review. So every place where an AI-written sentence touches the screen is a risky place. Instead of filtering the input, I sealed the spot the sentence goes into. Filtering loses to the next workaround.

## Rabbit holes

Video production failed 3 times in a row. A check I added the day before was the cause. I put it in a file the video side also reads, and the program that draws the video can't read one of the parts inside it. The code review passed fine; this only showed up when a video was actually built. I moved the check elsewhere and built a small inspection that catches the same mistake in advance.

The job that refreshes the project list every 30 minutes was also failing. Each run changes 2 files, but the save step only picked up 1, and the leftover file made the save get rejected. Since nothing saved, the next run died at the same spot again. The failure kept re-creating itself.

## Next up

- Pick candidates for moving drawings in scenes (the tool that filters usable ones is ready)
- Keep checking that the recording actually uses the screen the script picked
